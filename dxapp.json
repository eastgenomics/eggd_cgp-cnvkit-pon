{
  "name": "eggd_cgp-cnvkit-pon",
  "title": "eggd_cgp-cnvkit-pon",
  "summary": "Builds a CNVkit panel-of-normals (PoN) reference from per-sample coverage files produced by eggd_cgp-cnvkit-coverage. Output reference.cnn is the input to eggd_cgp-cnvkit-batch.",
  "dxapi": "1.0.0",
  "version": "2.1.0",
  "properties": {
    "githubRelease": "2.1.0"
  },
  "inputSpec": [
    {
      "name": "coverage_files",
      "class": "array:file",
      "label": "Per-sample coverage files (.targetcoverage.cnn)",
      "patterns": [
        "*.cnn"
      ],
      "help": "One .targetcoverage.cnn file per sample, produced by eggd_cgp-cnvkit-coverage. Minimum 10 files recommended; 20+ preferred for a stable PoN median. All files must have been generated with the same BED and target_avg_size settings."
    },
    {
      "name": "pon_name",
      "class": "string",
      "label": "Output reference filename stem (no extension)",
      "default": "cgp_cnvkit_reference",
      "optional": true,
      "help": "Base name for the output .cnn reference file. Default: cgp_cnvkit_reference."
    },
    {
      "name": "fasta",
      "class": "file",
      "label": "Reference genome FASTA (.fasta.gz, bgzf-compressed)",
      "patterns": [
        "*.fasta.gz",
        "*.fa.gz"
      ],
      "optional": true,
      "help": "When supplied, cnvkit.py reference annotates each interval with GC content and RepeatMasker fraction, enabling GC-bias correction in downstream fix steps. The FASTA chromosome naming must match the BED/BAM naming (chr-prefix for chr-prefix BAMs). Requires fasta_fai and fasta_gzi to be supplied alongside."
    },
    {
      "name": "fasta_fai",
      "class": "file",
      "label": "FASTA index (.fasta.gz.fai)",
      "patterns": [
        "*.fai"
      ],
      "optional": true,
      "help": "samtools .fai index for the bgzf-compressed FASTA. Required when fasta is supplied."
    },
    {
      "name": "fasta_gzi",
      "class": "file",
      "label": "FASTA bgzf block index (.fasta.gz.gzi)",
      "patterns": [
        "*.gzi"
      ],
      "optional": true,
      "help": "bgzf block index (.gzi) for the bgzf-compressed FASTA. Required when fasta is supplied."
    }
  ],
  "outputSpec": [
    {
      "name": "cn_reference",
      "class": "file",
      "label": "CNVkit PoN reference (.cnn)",
      "patterns": [
        "*.cnn"
      ],
      "help": "Pooled PoN reference file encoding per-interval median and spread across all input samples. Pass to eggd_cgp-cnvkit-batch as cn_reference."
    },
    {
      "name": "reference_stats",
      "class": "file",
      "label": "Reference summary statistics (.tsv)",
      "patterns": [
        "*.tsv"
      ],
      "help": "TSV of summary metrics: n_intervals, log2_mean, log2_median, log2_stdev, depth_median, n_samples. Use to verify the PoN was built correctly."
    }
  ],
  "runSpec": {
    "timeoutPolicy": {
      "*": {
        "hours": 2
      }
    },
    "distribution": "Ubuntu",
    "release": "24.04",
    "version": "0",
    "interpreter": "bash",
    "file": "src/code.sh"
  },
  "developers": [
    "org-emee_1"
  ],
  "authorizedUsers": [
    "org-emee_1"
  ],
  "regionalOptions": {
    "aws:eu-central-1": {
      "systemRequirements": {
        "*": {
          "instanceType": "mem2_ssd1_v2_x4"
        }
      }
    }
  },
  "access": {
    "allProjects": "VIEW"
  }
}
